"""Constants for {{ integration_name }}."""

from __future__ import annotations

from homeassistant.const import Platform

DOMAIN = "{{ domain }}"

PLATFORMS: list[Platform] = [
{%- for p in platforms.split(',') %}
    Platform.{{ p | trim | upper }},
{%- endfor %}
]
